# 2026-07-15 — Session wrap: QA backlog → issues, nightshift model delegation

**Operation:** log (session wrap)
**Scope:** plans/qa-feature-backlog (issues filed), tooling (nightshift skill)

Fills the gap after the per-item logs ([init](2026-07-14-init.md), [neo-write-fix](2026-07-14-neo-write-fix.md), [neo-scaffold-fix](2026-07-14-neo-scaffold-fix.md)) — the live-QA fixes for items 1–3 are already logged there.

## What happened (this half of the session)

- **Backlog → GitHub issues.** Filed the remaining backlog as 9 issues on the fork (`Two-Rivers-Marketing/craft-mcp-2rm` #18–#26), each with a user story, checkbox success criteria, agent instructions, an environment note (which need live mbd+MCP), and a `model:<name>` delegation label. Did NOT duplicate pre-existing #13–#16. Armed 8 with `ready-for-agent`; held #20 (blocked on #19).
- **nightshift skill overhaul** (`~/.claude/skills/nightshift/` — personal tooling, not this repo): it ignored the `model:` label entirely (no `--model`). Added model-label delegation + per-issue model logging, `composer test` detection, in-worktree dependency install, worker/orchestrator duty split (worker posts findings, orchestrator verifies + closes), per-issue close (was batch-at-end), and `origin`-based repo resolution with `--repo` on every gh call.
- **Model-adherence test.** Ran nightshift against #13 (fable) + #14 (sonnet). Proved delegation works end-to-end (fingerprint probe: `--model fable/sonnet/opus` → distinct models). Both shipped on branch `nightshift/2026-07-15-0353`, findings-commented, closed.

## Decisions

- Fork stays nested in `mbd/_plugins/` (own git repo, symlinked path repo); mbd runs the fork; versioned dependency deferred.
- Per-issue close over batch-close in nightshift (crash resilience for AFK).
- Separation of duties: task agent reports findings; orchestrator verifies + closes.

## Gotchas worth remembering (mined for next time)

- `gh` defaults to the **upstream** repo in a fork — always `--repo`. Nearly filed issues on the public upstream.
- The Bash tool runs **zsh** (no unquoted-var word-splitting) — `$FLAG` tricks that work in bash break.
- Symlinking `vendor/` into a git worktree breaks Pest (namespaces from realpath) — install deps in-place.

## Next

- Commit the nightshift SKILL.md changes.
- QA + merge `nightshift/2026-07-15-0353`; push `main` (unpushed).
- Run the armed queue; #18 (get_form notifications/connections) is the highest-value remaining.
