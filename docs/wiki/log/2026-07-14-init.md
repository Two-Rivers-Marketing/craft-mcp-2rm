# 2026-07-14 — Wiki init + Freeform live-QA seed

**Operation:** init + ingest (first content)
**Scope:** overview, architecture, plans, decisions

## What happened

Bootstrapped the project wiki for `craft-mcp-2rm` at `docs/wiki/` (product docs in `docs/` left as-is, out of scope). Seeded with real content from the first live-QA session, not placeholders.

Pages created:
- `overview/project.md` — fork lineage, what the fork adds, the live-QA effort + priority order, how mbd consumes it (symlink path repo; SIGHUP-to-reload).
- `architecture/freeform-integration.md` — verified Freeform 5.15 read API, the 4 bugs found & fixed this session (non-public field props → getters; `isRequired`; submission value via `$sub->{handle}->getValue()`; `count()` string + `strict_types` `TypeError` swallowed to null), and the psysh-is-coercive lesson.
- `plans/create-form-tool.md` — scope for a minimal Freeform `create_form` write tool (the "can you make forms?" ask).
- `plans/qa-feature-backlog.md` — running to-do list for features/fixes surfaced during QA.
- `decisions/deferred/2026-07-14-freeform-write-tools.md` — defer form creation; build write features incrementally through QA.

## Context

Session was mid-way through Freeform live-QA (item 1 of 5). Freeform code fixes committed to `FreeformSerializer.php` / `FreeformTools.php` (unit suite 417 → 421 green). Live re-verify of the `submissionCount` cast was pending a server SIGHUP restart at time of writing.

## Next

- Reconnect craft-mcp, confirm `submissionCount` returns real numbers.
- Item 2: Neo multi-level tree writes.
- Backlog: `get_form` notifications/connections still null (high), `create_form` (scoped).
