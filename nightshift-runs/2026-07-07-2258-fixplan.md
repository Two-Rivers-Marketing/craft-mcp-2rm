# Fix Plan — 2026-07-07-2258

Discovered issues that need attention but are NOT the current task.
Append here instead of scope-creeping into unrelated fixes.
Format: one item per entry with a title and 1-2 lines of context.

## composer analyse OOMs at default 128M memory limit
`composer analyse` (phpstan) crashes with "reached configured PHP memory limit: 128M" on host PHP — pre-existing before #5. `vendor/bin/phpstan analyse --memory-limit=1G` passes clean. Fix: add `--memory-limit` to the composer script.

## Stale stimmt package refs in README, docs, CHANGELOG, release workflow, composer extra
README.md (packagist badges, `composer require stimmt/craft-mcp`, vendor paths), docs/README.md, docs/installation.md, CHANGELOG.md upstream issue links, .github/workflows/release.yml line 56 (`composer require stimmt/craft-mcp:...`), and composer.json `extra.developer`/`extra.documentationUrl` still reference stimmt. Needs editorial rebrand decisions (badges/packagist don't apply to private fork), not mechanical rename.

## composer lint:test fails on pre-existing tool files
`composer lint:test` (pint --test) fails on DebugTools.php, GraphqlTools.php, TinkerTools.php, DatabaseTools.php (class_definition/braces_position/ordered_imports) — pre-existing before #6. Run `vendor/bin/pint src/tools` once and commit.

## Built-in template stub extends path may not resolve on mbd
BlockTypeStub's default stub extends `global/_includes/body-block-open` (bank-iowa pattern per issue #11 spec), but mbd's real body_blocks templates extend `site-toolkit/global/_includes/body-block-open`. If live QA shows the stub 500s on mbd, either ship an mbd stub via the new `templateStubPath` setting (config/mcp.php) or change the built-in default.
