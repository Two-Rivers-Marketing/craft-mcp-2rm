# Nightshift Progress — 2026-07-07-2258

Append-only notes for the next issue in this run.
Write what the next person needs to know, not what you did.

## Issue #5 (namespace rename) — context for next issue
- Namespace is now `twoRivers\craft\Mcp` everywhere (src, tests, bin, composer.json PSR-4 + extra.class, phpstan.neon ignore pattern, docs/extending.md). Sub-namespaces are lowercase: `tools`, `services`, `support`, `installer`, `completions`, `resources`, `prompts`, `events`, `enums`, `models`, `attributes`, `contracts`, `console\controllers`.
- Namespace appears in STRING form in: composer.json (escaped `\\`), phpstan.neon `ignoreErrors` regex (`#Class twoRivers\\craft\\Mcp\\tools\\SchemaTools not found#`), bin/mcp-server (`Craft::$container->set(\twoRivers\craft\Mcp\tools\TinkerTools::class)` around line 113), tests/ArchTest.php (arch presets pin the root namespace). New tool classes must live under `twoRivers\craft\Mcp\tools` and are registered via ToolRegistry / RegisterToolsEvent.
- Installer bin path: `AbstractMcpClient::BIN_PATH = 'vendor/2rm/craft-mcp/bin/mcp-server'` — this is what gets written into .mcp.json / client configs. If package name ever changes, change it here.
- Run `composer dump-autoload` after any namespace/class moves; tests fail confusingly otherwise.
- Verification: `composer test` (Pest, 177 tests) is fast (<1s). phpstan needs `vendor/bin/phpstan analyse --memory-limit=1G` (composer script OOMs at 128M — see fix_plan.md).
- Deferred to human QA (needs live mbd Craft install): DDEV PHP bump, path-repo wiring of 2rm/craft-mcp into the mbd composer.json, live MCP server verification against the running site.

## Issue #6 (Neo schema tools) — context for next issue (Neo WRITE tools)
- `src/tools/NeoSchemaTools.php` has the field-resolution logic you should reuse: `resolveBuilderField(?int $entryId, ?string $fieldHandle)` — handle precedence is explicit param > `Mcp::settings()->builderFieldHandle` (new Settings prop, default 'contentBuilder', overridable via config/mcp.php). With entryId it walks the entry's field layout custom fields, prefers a handle match, falls back to the single Neo field, errors listing handles if ambiguous. Extract to a shared support class (or trait) if the write tools need it — it's private right now.
- Neo classes are imported at top of NeoSchemaTools (`use benf\neo\Field as NeoField; use benf\neo\Plugin as Neo;`) — PHP resolves lazily so this is safe when Neo is absent; `isAvailable()` guards with class_exists FIRST (before any Craft::$app access), so it's even callable in unit tests without Craft booted. Mirror this exactly. phpstan.neon now ignores `#benf\\neo\\#`.
- `src/support/NeoSerializer.php` (final, static) is fully duck-typed (no Neo imports) and reusable for write-tool responses: `blockType()`, `nesting()`, `fieldLayoutFields()` (reads required flags via FieldLayout::getCustomFieldElements, falls back to getCustomFields), `field()`, `fieldOptions()` (Dropdown/Radio/Checkboxes/MultiSelect via `options` property; Lightswitch special-cased by class-name suffix to true/false + on/off labels; optgroups skipped; bare-string options normalized). Private `prop()` handles public props, getters, and Yii magic getters; missing props → null (old Neo versions lack description/minSiblingBlocks).
- Template check: `Craft::$app->getPath()->getSiteTemplatesPath() . '/body_blocks/<handle>.twig'` via is_file, reported as `template: {path, exists}` on each block type.
- For WRITE tools you'll need `benf\neo\elements\Block` — create via `new Block()` with `fieldId`, `typeId` (BlockType->id), `ownerId`, `level`, `sortOrder`? No — Neo blocks are saved through the owner's field value (Craft 5 canonical→derivative indirection, see mbd docs/wiki/recipes/neo-content-queries.md). Verify against a live install; nothing here could be tested against real Neo (not installed in this env).
- phpstan custom rules bite: NoElse, NoNestedIfStatements, NoNestedLoops, RequireGuardClauses. Write guard-clause style or phpstan fails. Run `vendor/bin/phpstan analyse --memory-limit=1G` (composer script still OOMs).
- `composer lint:test` (pint) FAILS on pre-existing files (DebugTools, GraphqlTools, TinkerTools, DatabaseTools) — not ours; logged in fix_plan.md. Pint your own new files individually: `vendor/bin/pint <files>`.
- Tests: 202 passing (was 177). Structural pattern: tests/Unit/Tools/NeoSchemaToolsTest.php; pure-logic serializer pattern with anonymous classes (+ `#[AllowDynamicProperties]`): tests/Unit/Support/NeoSerializerTest.php.
- Deferred to human QA: running describe_content_builder/get_block_type against the live mbd install (Neo + real contentBuilder field; note mbd's builder field handle may be `neoContentBuilder` — set `builderFieldHandle` in config/mcp.php there).

## Issue #7 (create_neo_block) — context for #9 (children trees + positioning) and #10 (update/reorder/delete)
- New tool class: `src/tools/NeoContentTools.php` — `create_neo_block(entryId, blockType, ?fieldHandle, ?fields JSON, dryRun=false)`. Conditional registration in ToolRegistry::collectCoreTools (its isAvailable() just delegates to NeoSchemaTools::isAvailable()). dangerous: true, ToolCategory::CONTENT. #9/#10 tools go in this same class.
- Field resolution is now a shared trait: `src/support/ResolvesNeoBuilderField.php` (`resolveBuilderField(?entryId, ?fieldHandle)`, `resolveFieldFromEntry()`, `assertNeoAvailable()` via `static::isAvailable()`). Both NeoSchemaTools and NeoContentTools `use` it. Trait lives in support/ (NOT tools/) because tool discovery scans the tools subdir.
- Payload/diff support: `src/support/NeoBlockPayload.php` (final, static, zero Craft/Neo deps — extend, don't rewrite):
  - `decode(?string $fieldsJson): array` — JSON object => handle=>value map; null/blank/`{}` => []; throws ToolCallException on invalid JSON, JSON lists, scalars.
  - `unknownHandles(fields, allowedHandles)` / `assertKnownHandles(fields, allowedHandles, blockTypeHandle)` — validation error lists unknown + available (`(none)` when block type has no fields).
  - `summarizeBlock(object $block)` — duck-typed {id, type (via getType()->handle, null on throw), level, enabled}. For #9 children: level is already carried.
  - `diff(currentSummaries, appendedBlock)` — {before: {blockCount, blocks}, after: {...}, appended}. #9 positioning: replace the append-at-end with an insert-at-index builder; #10: pass the mutated list instead. Both can keep the before/after envelope shape.
- Write path in NeoContentTools (all Neo access duck-typed or via lazy `benf\neo\elements\Block` / `benf\neo\Field` imports; nothing here ran against real Neo — env has no Neo install):
  1. `resolveCanonicalOwner(entryId)` — getElementById then `$entry->getCanonical()`; ALWAYS write to canonical (Craft 5 canonical/derivative).
  2. `resolveBlockType(field, handle)` — from `$field->getBlockTypes()`, error lists available.
  3. allowed handles from block type layout via NeoSerializer::fieldLayoutFields.
  4. `existingBlocks(owner, handle)` — getFieldValue; if query: `->status(null)->all()` (all levels, all statuses — sortOrder counts nested children too).
  5. `saveBlock()` — new Block with fieldId/typeId/ownerId/siteId/level=1/sortOrder=count+1/enabled; sets `primaryOwnerId` if canSetProperty, calls `setOwner($owner)` if method exists; `saveElement($block)`, ToolCallException with getErrors() on failure.
  6. `integrateBlockIntoOwner()` — `$owner->setFieldValue($handle, [...existing, $block])` then `saveElement($owner)` so Neo rebuilds field structure (mirrors Neo Field::saveValue).
- ASSUMPTIONS NEEDING LIVE QA (mbd install, scratch entry):
  - Double-save concern: block is saved directly, then owner resaved with block in value. Assumed idempotent (block has id => Neo updates + builds structure, no duplicate). If live QA shows duplicates, drop step 5's direct saveElement and let the owner resave persist the new (unsaved) block.
  - Neo Field::normalizeValue accepting a mixed array of saved Block elements — unverified. Fallback if it chokes: duck-typed `Neo::$plugin->fields->saveValue($field, $owner)` after direct block save, skipping setFieldValue.
  - `primaryOwnerId`/`setOwner` existence on mbd's Neo version (Craft 5 / Neo 5.x expected — both should exist).
  - sortOrder counting nested children (flat query count) matches Neo's structure append semantics.
  - Block renders live in body_blocks template after save (canonical write, Blitz cache may need warm/clear).
- Tests: 230 passing (was 202). New: tests/Unit/Tools/NeoContentToolsTest.php (structural reflection: dangerous flag, category, signature, tool count), tests/Unit/Support/NeoBlockPayloadTest.php (pure-logic: decode/validate/summarize/diff). phpstan clean at 1G; pint clean on new files (pre-existing lint:test failures remain, see fix_plan.md).

## Issue #8 (upload_asset) — context for #9 (Neo children trees use returned asset IDs as field values)
- New tool: `AssetTools::uploadAsset(path, volume, ?folder)` in `src/tools/AssetTools.php` (5th tool on this class now, alongside list_assets/get_asset/list_volumes/list_asset_folders). dangerous: true, ToolCategory::CONTENT — plain instance method, no new registration needed (AssetTools is already an unconditional core tool in ToolRegistry).
- Flow: `assertReadableFile(path)` (is_file + is_readable, guard clause) -> resolve `Volume` via `getVolumeByHandle` (error lists available handles via `availableVolumesMessage()`) -> `resolveTargetFolder()` (root folder via `getRootFolderByVolumeId` when `folder` is null/blank; otherwise `Assets::ensureFolderByFullPathAndVolume($folder, $volumeModel, false)` — the `false` for `$justRecord` also creates the physical directory, not just the DB record) -> `copyToTempPath()` copies the source into `Craft::$app->getPath()->getTempAssetUploadsPath()` with a `FileHelper::uniqueName()`'d filename (Asset's `_validateTempFilePath()` rejects tempFilePath outside known temp roots/project root — never point tempFilePath at the caller's original file) -> `buildAsset()` sets `tempFilePath`, `filename` (sanitized via `sanitizeFilename()` — strips dir components, `[^A-Za-z0-9._-]` -> `_`, falls back to `'upload'` if empty), `newFolderId`, `avoidFilenameConflicts = true`, `setScenario(Asset::SCENARIO_CREATE)` -> `Craft::$app->getElements()->saveElement($asset)`, surfaces `$asset->getErrors()` in ToolCallException on failure. Response reuses existing `serializeAsset()` (id/title/filename/kind/size/width/height/url/volumeId/folderId/dates) — width/height come through populated already for images, no `detailed` flag needed.
- For #9: the returned `asset.id` is exactly what you'd feed into a Neo Assets-type field's value array (Craft asset fields normalize `[id, id, ...]` or asset-query-like arrays — check `NeoSerializer::field()`/`fieldOptions()` output from #6 for how field types are already being introspected, and `NeoBlockPayload::decode()`/`assertKnownHandles()` from #7 for how field values flow into `create_neo_block`'s `fields` JSON). No new dependency between #8 and #9's core logic — just a data value now available.
- Tests: `tests/Unit/Tools/AssetToolsTest.php` — structural reflection (tool attribute, dangerous+category, signature: path/volume/folder/context) + pure-logic reflection-invoke tests for `assertReadableFile` and `sanitizeFilename` (no Craft boot; instantiates `new AssetTools()` and reflects into the private methods — no `setAccessible(true)` call needed/wanted, it's a deprecated no-op as of PHP 8.5 on this host and trips `failOnDeprecation`-style strictness). 242 tests passing (was 230).
- phpstan clean at `--memory-limit=1G` (custom guard-clause rules respected: no `else`, no nested `if`, small single-purpose private helpers). `vendor/bin/pint --test` clean on both new/touched files.
- DEFERRED TO LIVE QA (no live mbd Craft install in this env): actual upload against a real mbd volume (Google Cloud Storage-backed — check whether `ensureFolderByFullPathAndVolume(..., false)` correctly round-trips physical directory creation on that FS type, some remote FS adapters treat "directories" as prefix-only and may no-op or error on `createDirectory`); whether `avoidFilenameConflicts` behaves as expected when uploading a file whose sanitized name collides with an existing asset; end-to-end timing/size limits for large files copied through `getTempAssetUploadsPath()`.

## Issue #9 (nested trees + positioning) — context for #10 (update/reorder/delete)
- create_neo_block now takes: `createNeoBlock(entryId, blockType, ?fieldHandle, ?fields, ?children, ?position, ?parentBlockId, dryRun=false, ?context)` (9 params). `children` = JSON array of recursive nodes `{blockType, fields, children}`; `position` = integer index OR `before:<id>` / `after:<id>`; `parentBlockId` = int, nest inside an existing block.
- **REUSE THIS FOR #10**: new pure-logic sibling class `src/support/NeoBlockTree.php` (final, static, zero Craft/Neo deps). Position resolution and flat-list traversal are all here and directly reusable by reorder/move/delete:
  - `subtreeEnd(flat, start)` — index just past the subtree rooted at `start` (uses `level` to find where descendants end). This is THE primitive for moving/deleting a whole block+descendants range.
  - `findIndexById(flat, id)` — flat index of a block by id, or null.
  - `resolveInsertionIndex(flat, position, rangeStart, rangeEnd, level)` — resolves null/int/before:/after: to a flat insertion index within a sibling scope. Top-level scope = (0, count, 1); parent scope = (parentIndex+1, subtreeEnd(parentIndex), parentLevel+1). before/after reference IDs must be siblings *in that scope* (level match), else "not an existing sibling" error; nested/unknown IDs throw; out-of-range ints throw (not clamped, per issue).
  - `flatten(node, level)` — preorder flat list `[{type, level, fields}]` with incrementing levels. `normalizeTree(rootType, rootFields, rawChildren)` + `decodeChildren(json)` validate/normalize recursively (malformed payloads throw with the node path e.g. `block.children[0].children[1]`).
  - `parentAllowsChildren(childBlocks)` / `childBlocksAllows(childBlocks, type)` — Neo childBlocks rule check. Lenient on unreadable `null` (treats as allowed), only explicit `false`/`[]` hard-disallows. `'*'`/true = any; array = handle membership.
  - `diff(beforeFlat, insertionIndex, insertedFlat)` — {before, after, inserted{at,blockCount,blocks}} with inserted blocks marked `id:null, new:true`. #10 can build before/after by passing a mutated flat list (for delete/reorder) — same envelope.
- NeoContentTools now: `validateTree(field, node)` recursively resolves each node's block type + asserts known field handles + checks childBlocks permits its children. `resolveScope()` handles top-level vs parentBlockId (validates parent exists + `assertParentAccepts` its childBlocks allows the root type). Write path CHANGED from #7: no longer direct-saves the block then resaves owner. Now builds the WHOLE tree as unsaved leveled `Block` objects (`buildTreeBlocks`/`buildBlock`, level set, no sortOrder — Neo recomputes structure from ordered array + level), splices into the owner's existing flat block list at the insertion index, single `setFieldValue` + `saveElement($owner)` (`persistTree`). This is the #7 "fallback" path the earlier notes predicted; #10 should follow the same splice-then-resave-owner model (delete = filter out a subtree range; reorder = array_splice source range to dest index; then setFieldValue full ordered list + save owner).
- LIVE QA MUST EXERCISE (highest priority — none of the Neo write path ran against real Neo here):
  - Multi-level tree save: build a real multiColumn with 2+ columnItem children (+ a grandchild block) from the PRD screenshot workflow; verify **sortOrder/lft-rgt/level integrity** after `saveElement($owner)` — confirm Neo's `normalizeValue`/`saveValue` accepts a flat ordered array mixing saved existing `Block` elements and NEW unsaved `Block` elements carrying only `level` (no sortOrder), and rebuilds the nested structure correctly (no flattening, no orphaned/duplicate blocks).
  - Position: insert `before:`/`after:` an existing top-level block; integer index; and `parentBlockId` nesting into an existing block's children — verify the block lands at the exact spot and existing blocks/children keep order.
  - childBlocks validation against a real Neo BlockType: confirm `NeoSerializer::nesting()['childBlocks']` returns `'*'` / array-of-handles / false as expected on mbd's Neo version so `assertChildrenAllowed`/`assertParentAccepts` neither false-block legit nesting nor allow illegal nesting. mbd's builder field handle may be `neoContentBuilder` (set `builderFieldHandle` in config/mcp.php).
  - `blockIds` in the response are read from `$block->id` after the owner save — verify IDs populate (if not, may need to re-query the field value post-save to map new blocks to ids).

## After #10 (edit suite) — orchestrator note (agent report cut off by API error)
- #10 complete: `update_neo_block`, `reorder_neo_blocks` (full order OR single move), `delete_neo_block` — all dangerous, all dryRun. Commits 57ab2e6 (support primitives: reorder/move/delete + field-diff in NeoBlockTree/support), e5cf0ce (tools).
- For #11 (create_block_type): NeoSchemaTools has field/block-type resolution + NeoSerializer; Settings has builderFieldHandle. Scaffolding stub goes to body_blocks/<handle>.twig under getSiteTemplatesPath(); default stub = bank-iowa pattern (extends global/_includes/body-block-open, main-content containerClass wrapper, children loop for containers); config override via new setting (e.g. templateStubPath).
- For #12 (Freeform): mirror CommerceTools/NeoSchemaTools conditional pattern; Freeform classes under Solspace\Freeform\* (not installed here — duck-type).
- Live QA for #10: subtree move integrity (lft/rgt/level), permutation reorder on real structure, delete cascades, canonical routing from derivative-owned block lookups.

## Issue #11 (create_block_type) — context for #12 (Freeform tools, last issue)
- New tool class: `src/tools/NeoScaffoldTools.php` — single tool `create_block_type(name, ?handle, ?fieldHandle, ?existingFields, ?newFields, ?childBlockTypes, scaffoldTemplate=true, dryRun=false)`. dangerous: true, ToolCategory::SCHEMA, conditional in ToolRegistry::collectCoreTools (isAvailable() delegates to NeoSchemaTools). Commits 1ee937e (support), 4d2e5b4 (tool).
- Pure support (reusable, fully tested without Craft): `src/support/BlockTypeStub.php` (stub renderer; custom-template tokens __BLOCK_HANDLE__ / __FIELD_HINTS__ / __CHILDREN_LOOP__) and `src/support/FieldMatcher.php` (matchExisting: handle > type+normalized-name; closeCandidates: containment or levenshtein<=3, closest-first).
- New Settings prop `templateStubPath` (?string, default null = built-in stub), documented in docs/configuration.md. Field type map: plainText/dropdown/lightswitch/asset -> craft\fields\*; richText -> `craft\ckeditor\Field` (class_exists-guarded, clear error naming the plugin if absent).
- Write path (NONE of it ran against real Neo — env has no Neo): plan everything first (planBlockType: handle from StringHelper::toCamelCase, block-type-handle collision check, childBlockTypes validated against existing handles + the new handle for self-nesting, attachments deduped by handle, stub conflict check throws even on dryRun), then execute: saveField per created field -> FieldLayout(['type' => Block::class]) + FieldLayoutTab('Content') + CustomField elements with required flags -> new benf\neo\models\BlockType (fieldId/name/handle/enabled/topLevel/sortOrder=count+1/childBlocks) -> layout attached via $blockType->hasMethod('setFieldLayout') (FieldLayoutBehavior magic; fallback saveLayout + fieldLayoutId) -> Neo::getInstance()->get('blockTypes', false) service, save() with saveBlockType() fallback -> craft\helpers\FileHelper::writeToFile for the stub.
- FOR #12 (Freeform tools): mirror the CommerceTools/NeoSchemaTools conditional pattern exactly — isAvailable() with class_exists FIRST (Solspace\Freeform\Freeform), then plugin-enabled check; register conditionally in ToolRegistry::collectCoreTools; add phpstan ignore `#Solspace\\Freeform\\#` to phpstan.neon ignoreErrors (Freeform NOT installed in this env, same as Neo/Commerce). Structural test pattern: tests/Unit/Tools/NeoScaffoldToolsTest.php. Pint your own files individually (`vendor/bin/pint <files>`) — repo-wide lint:test still fails on pre-existing files. phpstan via `vendor/bin/phpstan analyse --memory-limit=1G` (composer script OOMs).
- Tests: 370 passing (was 334). phpstan clean; pint clean on all touched files.
- LIVE QA MUST EXERCISE for #11 (mbd install, ALLOW_ADMIN_CHANGES=true, builderFieldHandle likely `neoContentBuilder` in config/mcp.php):
  - Neo BlockTypes service save: confirm `Neo::getInstance()->get('blockTypes', false)` resolves and the service method is `save(BlockType)` on mbd's Neo version (saveBlockType fallback exists); confirm block type appears in CP under the builder field and Craft writes config/project/neo/blockTypes/*.yaml itself.
  - Field layout persistence: confirm `$blockType->hasMethod('setFieldLayout')` is true (FieldLayoutBehavior) and the layout (with required flags) survives the save + project-config round trip.
  - New-field creation: dropdown options land correctly; CKEditor richText field creation (mbd has CKEditor?); field-match reporting on a deliberate near-duplicate (e.g. newFields name "Heading" when a `heading` PlainText exists) — should attach, not create.
  - Stub renders: create a scratch entry with the new block type; verify body_blocks/<handle>.twig renders through the mbd layout — NOTE mbd extends `site-toolkit/global/_includes/body-block-open` while the built-in stub extends `global/_includes/body-block-open` (bank-iowa pattern per issue spec); if mbd resolution doesn't cascade, set templateStubPath in config/mcp.php to an mbd-specific stub.
  - dryRun parity: dryRun output matches what a real run then produces.

## Issue #12 (Freeform tools, last issue in this run)
- New tool class: `src/tools/FreeformTools.php` — 6 tools: `list_forms`, `get_form`, `list_submissions`, `get_submission`, `delete_submission` (dangerous), `export_submissions` (dangerous). Conditional pattern mirrors CommerceTools exactly (isAvailable: class_exists(Solspace\Freeform\Freeform::class) first, then isPluginEnabled('freeform'), then project-config fallback + loadPlugins() reload). Registered in ToolRegistry::collectCoreTools. phpstan.neon ignore added: `#Solspace\\Freeform\\#`.
- Confirmed via Freeform 5.x developer docs (fetched live, see commit): `Freeform::getInstance()->forms` (FormsService: getFormById/getFormByHandle/getAllForms), `Freeform::getInstance()->submissions` (SubmissionsService: getSubmissionById/getSubmissionByToken), and `Solspace\Freeform\Elements\Submission::find()` (a real Craft ElementQuery — supports `->formId()`, `->status()`, `->limit()`, `->offset()`, `->dateCreated()`, `->count()` like any Craft element). This part is solid.
- NOT part of Freeform's stable public API and UNVERIFIED (no Freeform install in this env): the shape of a form's field layout (`getLayout()->getFields()` vs `getFields()` directly — both are tried, in that order, with a raw-iterable fallback), notification settings, element connections (the feature that maps submissions to entry creation), and spam-protection settings. `get_form()` cannot name-check these precisely across Freeform versions, so it takes a generic approach instead: `FreeformSerializer::sectionAttributes($form, $keywords)` pulls `toArray()`/`getAttributes()`/public-property dumps and keyword-filters keys (substring match on 'notif', 'connect'/'integration'/'element', 'spam'/'honeypot'/'captcha'/'duplicate'/'blocklist'). This WILL need live-QA correction — likely renaming keywords or replacing with real getters once the actual FormModel/Form property names are seen on a booted mbd+Freeform install.
- `src/support/FreeformSerializer.php` (final, static, zero Freeform deps, fully duck-typed via `readProp()`/`readMethod()` — same prop-fallback pattern as NeoSerializer): `formSummary()`, `form()`, `fieldLayout()`/`fieldSummary()`, `submissionSummary()` (status via getStatus() object or scalar property, dateCreated via DateTimeInterface format), `submissionFieldValues()` (getFieldValue() first, falls back to direct property access — Freeform submissions may expose field values as magic/native properties matching field handles, unverified), `toCsv()` (pure, `fputcsv` against php://temp with explicit escape char to dodge the PHP 8.4+ fputcsv deprecation), `sectionAttributes()` (generic keyword-filtered attribute dump, used for notifications/connections/spamSettings).
- `export_submissions`: writes CSV to `getPath()->getTempPath() . '/freeform-exports/<file>.csv'` (created via `FileHelper::createDirectory`) and ALWAYS returns `path`; ALSO inlines `content` in the response when the CSV is <= 32KB (`INLINE_CONTENT_LIMIT` const). Only `format: csv` is supported — anything else throws a clear error. `since` filters via `dateCreated(['>=', $since])` if the query supports it (guard-clause skip otherwise).
- `list_forms`'s `submissionCount` does one `Submission::find()->formId($id)->status(null)->count()` per form (a single COUNT query, cheap at typical form-list sizes) — swallowed to `null` on any Throwable (e.g. if formId() isn't the right criteria name on mbd's Freeform version).
- Tests: 417 passing (was 370). `tests/Unit/Tools/FreeformToolsTest.php` (structural reflection: isAvailable, attributes, dangerous flags on delete/export only, signatures, 6-tool count, unavailable-without-Freeform). `tests/Unit/Support/FreeformSerializerTest.php` (pure-logic, anonymous-class duck-typed objects: readProp fallback chain, fieldLayout via both getLayout()->getFields() and direct getFields(), fieldSummary type resolution, submissionSummary status resolution + date formatting, submissionFieldValues both paths, toCsv header/missing-key/extra-key/quoting/JSON-encoding, sectionAttributes keyword filter + toArray() precedence + null-when-nothing-introspectable). phpstan clean at `--memory-limit=1G`; pint clean on all touched/new files.
- LIVE QA MUST EXERCISE (Freeform not installed in this env — nothing here ran against real Freeform; highest priority of all 12 issues to verify since the API surface beyond forms/submissions lookup was never confirmed against a real plugin instance):
  - `get_form`'s `fields`/`notifications`/`connections`/`spamSettings` against a real form — confirm `getLayout()->getFields()` (or `getFields()`) actually returns Freeform field objects with `handle`/`label`/`required` properties or getters, and check whether the generic keyword-filtered `sectionAttributes()` output for notifications/connections/spam is actually useful or just noise; if the real FormModel exposes e.g. `$form->notifications` or `$form->getAdminNotificationId()` directly, replace the generic dump with explicit reads.
  - Element Connections specifically — mbd's actual use case per the issue ("why didn't this submission create an entry") — confirm where that config lives on the form object; the generic 'connect'/'integration'/'element' keyword filter is a guess and may miss it entirely.
  - `list_submissions`/`export_submissions` status filtering: confirm `SubmissionQuery::status()` accepts Freeform's own status handles (not just Craft's native enabled/disabled), since Freeform submissions have their own StatusModel (Open/Closed/etc via a StatusesService, unconfirmed name).
  - `export_submissions` against a real form with many submissions: verify field handle-to-value mapping is correct in the CSV (not just IDs), and that `getPath()->getTempPath()` is writable on mbd's server (GCS-backed volumes shouldn't matter here since this writes to local temp, not a volume).
  - `delete_submission`: confirm `Craft::$app->getElements()->deleteElement($submission)` is the correct way to delete a Freeform submission (vs a dedicated `SubmissionsService::deleteSubmission()` call) — Submission is documented as a real Craft element via `::find()`, so this should be right, but unverified.

## Run summary — all 12 issues complete
What exists now in this fork (`twoRivers\craft\Mcp`) beyond the upstream craft-mcp base:
- **Commerce** (read-only, pre-existing): products, orders, statuses, product types.
- **Neo content-builder** (#6-#11, all conditional on benf/craft-neo): schema introspection (`describe_content_builder`, `get_block_type`), full write suite (`create_neo_block` incl. nested trees/positioning/parentBlockId, `update_neo_block`, `reorder_neo_blocks`, `delete_neo_block`), and scaffolding (`create_block_type` — creates the block type, attaches/creates fields, writes a body_blocks template stub, all in one call).
- **Assets** (#8): `upload_asset` added alongside pre-existing list/get/volumes/folders tools.
- **Freeform** (#12, conditional on solspace/craft-freeform): forms/submissions read tools + `delete_submission`/`export_submissions` writes.
- **Namespace** (#5): fully rebranded to `twoRivers\craft\Mcp` (was `stimmt\craft\Mcp` or similar upstream).
- Test suite: 417 passing (started this run at 177). phpstan clean at `--memory-limit=1G` throughout (composer's `analyse` script still OOMs at the default 128M — see fix_plan.md). `vendor/bin/pint --test` clean on every file touched across all 12 issues (repo-wide `composer lint:test` still fails on pre-existing files never touched this run: DebugTools, GraphqlTools, TinkerTools, DatabaseTools — logged in fix_plan.md, not fixed since out of scope for each issue).

Top live-QA priorities across all 12 issues, roughly in risk order (highest first):
1. **Freeform (#12)** — nothing ran against a real plugin instance; `get_form`'s notifications/connections/spamSettings sections are a best-effort generic dump and will very likely need real property names substituted in once a live Freeform form is inspected.
2. **Neo multi-level tree writes (#9/#10)** — `create_neo_block`'s tree-splice-then-resave-owner path (and reorder/delete sharing it) never ran against real Neo; verify sortOrder/lft-rgt/level integrity after save on a real multi-level builder (multiColumn + columnItem + grandchild).
3. **Neo scaffolding (#11)** — `create_block_type`'s Neo BlockTypes service save (`Neo::getInstance()->get('blockTypes', false)`), field-layout persistence, and template stub rendering through mbd's actual template inheritance chain (mbd uses `site-toolkit/global/_includes/body-block-open`, the built-in stub uses `global/_includes/body-block-open` — likely needs `templateStubPath` override).
4. **Asset upload (#8)** — GCS-backed volume folder creation and filename-conflict behavior on mbd's actual storage adapter.
5. **Neo childBlocks / positioning edge cases (#9)** — before:/after: siblings, parentBlockId nesting, and the childBlocks permission rule shape (`'*'` vs array vs false) against mbd's real Neo version.
6. **DDEV/path-repo wiring (#5)** — this fork was never wired into mbd's actual composer.json as a path repo; the MCP server has never been started against the live mbd Craft install at all. This is the prerequisite for every other item above.
